
export const metadata = {
  title: "Russian Helis — Mi-8 build supervision",
  description: "Mi‑8 build supervision: configuration, factory oversight, acceptance, entry into service. Marketplace for pre‑owned helicopters.",
};
import "./../styles/globals.css";
export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (<html lang="ru"><body>{children}</body></html>);
}
